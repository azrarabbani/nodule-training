/* global describe, before, after, it, define */
define(function () {
	"use strict";
	/**
	 * Verify that browser unit tests work
	 */
	describe('Mocha browser unit tests', function () {
		it('should work', function (done) {
			done();
		});
	});
});